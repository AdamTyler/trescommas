var React = require('react');
var Link = require('react-router').Link;

module.exports = venues = React.createClass({
  render: function() {
    return (
      <h1>
        VENUES!!!!
      </h1>
    );
  }
});
